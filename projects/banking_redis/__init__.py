"""Banking (Redis Eventing) project overlay.

This project reuses the base 'banking' model and replaces the Eventing stack
from Kafka to a Redis-backed queue to explore an architectural evolution.
"""
